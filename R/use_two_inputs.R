# This file describes a single function which is changed in different commits 
# for to a demonstration of the use of git submodules.

#' Add together two numbers.
#' 
#' @param x A number.
#' @param y A number.
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' use_two_inputs(1, 1)
#' use_two_inputs(10, 1)
use_two_inputs <- function(a, b){
  
  a + b
  
}
